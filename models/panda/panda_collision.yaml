directives:

# Add iiwa
- add_model:
    name: panda
    file: package://combining_kinematics/models/panda/panda_jrl.urdf
    # default_joint_positions:

- add_weld:
    parent: world
    child: panda::panda_link0
